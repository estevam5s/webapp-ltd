from setuptools import setup, find_packages

setup(
    name='meu-projeto',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'reportlab',
        # outras dependências do seu projeto
    ],
)
